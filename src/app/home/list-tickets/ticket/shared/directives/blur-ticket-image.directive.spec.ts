import { BlurTicketImageDirective } from './blur-ticket-image.directive';

describe('BlurTicketImageDirective', () => {
  it('should create an instance', () => {
    const directive = new BlurTicketImageDirective();
    expect(directive).toBeTruthy();
  });
});
