import { UneSurDeuxPipe } from './une-sur-deux.pipe';

describe('UneSurDeuxPipe', () => {
  it('create an instance', () => {
    const pipe = new UneSurDeuxPipe();
    expect(pipe).toBeTruthy();
  });
});
